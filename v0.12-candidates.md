# v0.12-candidates — HARVEST (cycle ouvert 23/09/2026)

> **Statut : HARVEST + PROPOSITIONS (§6) — rien n'est tranché, rien n'est screené.** Amorce du cycle v0.12.
> Source unique à ce stade : le journal de calibration R19 (privé), 20/08 → 23/09/2026, dont les
> classes ont été distillées le 23/09 lors de la scission du journal hors de la règle chargée.
> **Manquent avant toute promotion** : l'audit de falsification R18(c) (dernière exécution
> 17/07, trimestrielle → due au plus tard le 17/10), la re-mesure des instruments, le screen
> challenger de chaque candidat, et le harvest des session logs et de PILOTAGE-IA sur la période.
> Les N ci-dessous sont comptés sur le journal seul : ce sont des occurrences datées, pas encore
> des incidents vérifiés un par un. Le détail (dossiers, montants, PR) reste dans le journal privé.

## 1. Promotion ou amendement de règles existantes (N ≥ 3 au journal)

1. **Am.R19 — cadence « une passe par défaut » entre dans la norme Live.** Elle a été décidée le
   20/08 « hors cycle de version » et n'existe que dans la règle privée. Mesures depuis : les passes
   supplémentaires ont surtout réparé les précédentes ou tourné sur un bloc déjà saturé. Corollaire à
   promouvoir avec elle : **quand les régressions se concentrent sur un bloc, l'énumération exhaustive
   du bloc remplace la passe suivante** (31/08, 08-09/09 — où l'énumération a trouvé 2 défauts
   qu'aucune des 3 passes n'avait vus —, 10-11/09, 14/09). N ≥ 4.

2. **R19 clause privilèges sort de `[provisional]`, et s'amende.** N ≥ 4 (19/08, 31/08, 01/09 ×2,
   22/09). Amendement mesuré le 22/09 : après `REVOKE ALL … FROM PUBLIC`, `anon` garde EXECUTE —
   les default privileges l'accordent **nominativement** à la création. Les deux REVOKE sont dus
   (PUBLIC **et** anon/authenticated), et seule la sonde `has_function_privilege` fait foi.

3. **Am.R19 déclencheur — les diffs d'affichage mordent, mais sur l'instrument.** N = 4 (18/08,
   18/09, 22/09, 23/09) : diffs hors déclencheur strict (lecture pure, copy, badge), review lancée
   quand même, morsure à chaque fois — trois fois sur le filet de test plutôt que sur le code livré,
   une fois sur le périmètre d'un agrégat de travail. Question à trancher : élargir le déclencheur à
   « affichage de données d'argent ou de contrat », ou consigner que la review y a une autre valeur
   (vérifier l'instrument) sans l'imposer.

4. **Am.R4 — élargir un gate, c'est ouvrir tous les chemins qu'il fermait.** La phase-0 d'Am.R4
   énumère les lecteurs/écrivains d'un état ; elle doit aussi s'appliquer quand on **élargit un
   prédicat, une whitelist ou une garde**, et quand on retire une garde périmée (mesurer ce qu'elle
   masquait avant de la retirer). N = 4 (09/09, 10-11/09, 11/09, 14/09 — ce dernier : un correctif
   qui fait aboutir un appel externe rend atteignable tout le code d'après-succès).

5. **Am.R4 — comparer les prédicats des lecteurs, pas seulement les recenser, et le faire sur l'état
   APRÈS les autres correctifs de la même passe.** N = 3 (10/09 composition de deux correctifs
   justes en faille ; 11/09 deux lecteurs d'une même constante scopant sur deux mailles ; 21/09).

6. **Am.R17 — un filet doit rougir sur la neutralisation et le déplacement, pas seulement sur la
   suppression.** N ≥ 4 (03/09, 18/09, 22/09 ×2). Chaîne présente ≠ condition qui la déclenche ;
   compteur global ne distingue pas 1+1+1 de 3+0+0 ; `toContain` se satisfait d'un texte qui a
   changé de bloc. Corollaire : la mutation de falsification est **vérifiée appliquée** (contrôle
   discriminant, ancré sur le bloc muté) avant lecture du verdict (10/09, 21/09, 22/09), et une
   mutation verte est tranchée « trou » ou « mutant équivalent », jamais laissée ambiguë (10/09, 22/09).

7. **Am.R17 — un oracle dérivé du SUT ne mesure rien.** N ≥ 4 (18/08, 31/08, 10-11/09, 22/09) : liste
   recopiée, constante partagée par l'oracle et le code, test qui n'importe rien du sujet et finit
   cité comme preuve. Et toute assertion sur un fichier source écarte les commentaires, JSX compris
   (10/09, 18/09).

8. **Am.R17(3) — l'état terminal d'un livrable visible, c'est « rendu à l'écran ».** N ≥ 4 (18/08,
   02-03/09, 08/09, 21/09) : valeur calculée et jamais rendue, composant sans appelant, consigne
   recommandant un geste qui n'existe pas. `tsc` et la suite ne voient rien de cette classe.

9. **Am.R10 — une LECTURE dont l'erreur n'est pas lue est un échec silencieux.** R10 couvre
   `await mutation()` sans `{ error }` ; la classe mesurée est la lecture : timeout ⇒ données vides
   ⇒ « rien à faire » ⇒ la garde passe, en vert (02/09, 03/09, 10/09, 21/09). N ≥ 4. Distinguer
   l'inconnu du négatif dans le type de retour.

10. **Am.R1 — une justification écrite dans le code est une claim.** Docstring, commentaire, message
    de commit ou en-tête de module qui affirme une garde, une provenance ou un invariant : il se
    vérifie comme un « tests verts », et il est pire qu'absent quand il est faux, parce que le lecteur
    suivant croit le chemin analysé. N ≥ 5 (18/08, 09/09, 14/09, 22/09 ×2 dont une provenance inventée).

11. **Am.R1 — un agrégat n'est pas une mesure tant que son périmètre et son unité ne sont pas dits.**
    Agrégat sur un statut technique lu comme un agrégat d'argent ; agrégat sans borne temporelle ;
    agrégat de lignes lu comme agrégat de dossiers. N = 4 (21/09, 22/09 ×2, 23/09) — trois fois en
    position d'alerte, là où une fausse affirmation coûte le plus.

12. **Am.R1 — le code de sortie d'un outil n'est pas le verdict.** N ≥ 5 : `gh pr checks --watch`
    exit 0 sur check rouge (19/08) ; `git rebase … | tail` exit 0 sur conflit (23/09) ; `tsc` exit 134
    = OOM, pas des erreurs de type (18/09, 22/09) ; `gh pr merge` en erreur après avoir mergé (11/09) ;
    statut de job porté par sa dernière commande (22/09). Et le **taux de base** avant toute annonce
    de régression (au moins 6 fois dans le journal).

## 2. Candidats `[provisional]` (N = 2)

- **Reprise d'une PR ancienne : falsifier ses prémisses contre l'état actuel du repo** avant de la
  rebaser (22/09 ×2). Toute attribution de provenance se sonde par `git log -S`.
- **Garde positionnelle → restructurer en fonction exclusive par construction** plutôt qu'ajouter
  des assertions (22/09 ; forme voisine 31/08). La restructuration bat l'énumération quand la garde
  tient à l'ordre de deux branches.

## 3. Artefacts (hooks), hors norme

- **Hook R19 — faux positifs** : ≥ 4 en une journée le 31/08, puis 20/08, 23/09. Il cherche
  `SECURITY DEFINER` dans le contenu des fichiers et compare à `main` local (périmé). À resserrer sur
  les lignes `^+` du diff, les extensions `.sql`/`.ts`, et `origin/main`.
- **Hook `pre-bulk-mutation`** : apparie du texte, pas des statements (`DELETE` dans un corps de
  fonction, un littéral, un motif de recherche) — ≥ 6 faux positifs.
- **Séquence ADR** : `adr_next()` a rendu un numéro déjà pris (10/09) ; le test ne surveille que les
  collisions de fichiers, pas la dérive séquence/disque.

## 4. À instruire avant de trancher

- Audit R18(c) complet sur la période 17/07 → aujourd'hui (contradictions recherchées explicitement).
- Mortalité : R14 et R5 retirées le 20/08 ; 12 candidats d'amendement ci-dessus pour 0 retrait
  proposé — le cycle s'annonce en croissance nette. Chercher les retraits avant de promouvoir.
- Taille de la norme Live : ~3,5k tokens aujourd'hui. Plusieurs candidats (§1-6 à §1-8) sont des
  sous-clauses de R17 ; les fusionner en une clause plutôt qu'empiler.

## 5. Sondes de mortalité (23/09, repo de référence, 317 commits depuis la v0.11)

| Marqueur | Occurrences | Lecture |
|---|---|---|
| `[review-ok]` | 102 | R19 vivante |
| `[deploy-ok]` | 6 | garde vivante |
| `[autonomy-ack]` | 0 | mais compteur R15 : 85 avertissements émis — la garde parle, le bypass n'a jamais servi |
| `[workaround-assumed]` | **0** | alors qu'au moins un commit écrit « contournement assumé » **sans** le tag |
| `[provisional]` (commits) | 0 | le tag vit dans les fichiers de règles, pas dans les commits |
| `Snapshot` / `Cache` (R6) | 31 / 5 | R6 vivante |

Portée : comptage textuel sur messages de commit, pas un audit incident par incident.

## 6. Propositions (auteur : l'agent, 23/09 — à screener au challenger, puis à trancher)

Principe : **delta net ≤ 0 en nombre de clauses**, et plafond ~+300 tokens sur la norme Live.
On fusionne par règle d'accueil au lieu d'empiler douze amendements.

**P1 — PROMOUVOIR, fusionné dans R17 (candidats 6, 7, 8).** Réécrire le corollaire de R17 :
> *Corollary: a safety net must prove it BITES — on neutralisation and displacement, not only
> deletion: mutate the SUT, verify the mutation applied, observe the targeted red. An oracle
> derived from the SUT (shared constant, copied list, test importing nothing from its subject)
> measures nothing. For a visible deliverable, the terminal state is "rendered", not "returned".*
Remplace le corollaire actuel : +1 phrase nette.

**P2 — PROMOUVOIR, fusionné dans R1 (candidats 10, 11, 12).** Une seule clause :
> *Declared ≠ verified extends to three forms that read as evidence: prose in the code (a
> docstring, comment or commit message asserting a guard, invariant or provenance); an aggregate
> without stated perimeter and unit; a tool's exit code (read the state, not the return code).*

**P3 — PROMOUVOIR, fusionné dans l'amendement R4 existant (candidats 4, 5).** Compléter la phrase
phase-0 : « …re-list every reader and writer of that state — **and compare their predicates pairwise,
on the state after the pass's other fixes. Widening a gate, whitelist or predicate, or retiring a
stale guard, is a change to shared state: every path it closed opens.** »

**P4 — PROMOUVOIR dans R10 (candidat 9).** Remplacer « `await mutation()` without `{ error }` » par
« **a read or a mutation** whose `error` is not read — an unread read error makes a guard fail open ».
Zéro clause neuve, un mot de portée.

**P5 — R19 : deux promotions, un refus (candidats 1, 2, 3).**
- Clause privilèges **sort de `[provisional]`** (N ≥ 4) avec l'amendement « REVOKE from PUBLIC **and**
  from anon/authenticated; only `has_function_privilege` counts ».
- Cadence **« one pass by default; a saturated block calls for exhaustive enumeration, not another
  pass »** entre dans la norme.
- **Refus** d'élargir le déclencheur aux diffs d'affichage (candidat 3) : ce que la review y attrape,
  c'est le filet de test — c'est P1 qui couvre cette classe, pas un déclencheur plus large qui
  réintroduirait la cérémonie que v0.10 a retirée.

**P6 — RETIRER R18(a) (ghost-probes par cron).** « Prescribed, NOT wired » depuis le 17/07, soit
68 jours ; R18 dit elle-même qu'un déclaré-jamais-exécuté est un Cache sans rafraîchisseur. Soit on
la câble dans le cycle, soit elle sort — **proposition : sortir**, aucune morsure ne l'a réclamée.
R18(b) reste : le tag `[provisional]` est effectivement utilisé dans les fichiers de règles.

**P7 — METTRE SOUS SURVEILLANCE DE MORT la clause tag de R10 (`[workaround-assumed]`).** 0 usage en
317 commits, et au moins un contournement déclaré en toutes lettres sans le tag : la clause n'est
pas appliquée même quand son cas se présente. Garder l'interdit, poser une échéance (fin du cycle
suivant) : si toujours 0, retirer le tag et garder « nommer le contournement, sa portée et son
échéance dans le message ».

**Laissés au journal, hors norme** : les deux `[provisional]` du §2 (N = 2), et les artefacts du §3
(corrections de hooks — à faire comme commits d'outillage, sans cycle).

**Bilan de P1–P7** : 0 règle neuve, 5 amendements fusionnés dans des règles existantes, 1 retrait de
sous-clause (R18a), 1 surveillance de mort (R10 tag), 1 promotion hors `[provisional]`.

## 7. Décisions MF

Bloquantes pour la promotion : (1) valider le principe « delta net ≤ 0 » ; (2) P6 retrait ou
câblage ; (3) lancer le screen challenger sur P1–P5 et l'audit R18(c) (§4) avant de figer le texte.
